# Binaires laser (PC atelier)

`AYA_Laser_Sender.exe` — envoi TCP des codes (pause 10 s).

Rebuild : `build_laser_exe.bat` (recopie ici automatiquement).
Téléchargement prod : Dashboard → Impression laser → bouton vert exe.
